exports.getAllItemsAdmin = async (req, res) => {
  res.status(200).json({ message: "Admin endpoint ready", mode: "placeholder" });
};

exports.flagItem = async (req, res) => {
  res.status(200).json({ message: "Admin endpoint ready", mode: "placeholder" });
};

exports.adminDeleteItem = async (req, res) => {
  res.status(200).json({ message: "Admin endpoint ready", mode: "placeholder" });
};
